/*

renderDOM() will return some DIV to act as the root
renderData() will have its own ReactDOM.render() call
unrenderDOM() will be sure to clean up the local ReactDOM before letting the main one do its job

*/

export default () => {}
