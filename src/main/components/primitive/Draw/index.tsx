// Similar to Path but takes advantage of Canvas context's ability to change color/stroke/fill/whatever mid-path
// SVG rendering target will simply have to concatenate <path> elements on the page to achieve the same effect

export default class Draw {
}
